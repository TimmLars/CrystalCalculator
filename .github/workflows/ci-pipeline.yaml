name: CI Pipeline for docker
on: 
    push:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        outputs:
          IMAGE_NAME: ${{env.REPO }}
        steps:
            - name: Checkout the repo
              uses: actions/checkout@v6
            - name: Test checkout
              run: ls
            - name: Convert Repo name
              run: echo "REPO=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV" 
            - name: Build docker
              run: docker build -t ${{ vars.REGISTRY }}/${{ env.REPO }}:latest .
            - name: docker tag
              run: docker tag ${{ vars.REGISTRY }}/${{ env.REPO }}:latest ${{ vars.REGISTRY }}/${{ env.REPO }}:${{ github.sha }}
            - name: Test docker
              run: docker images
    push:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: print token
              run: echo ${{ secrets.REGISTRY_TOKEN }} ${{vars.REGISTRY_USERNAME }}
            - name: Login to Docker Hub
              uses: docker/login-action@v4
              with:
                  registry: ${{ vars.REGISTRY }}
                  username: ${{ vars.REGISTRY_USERNAME }}
                  password: ${{ env.REGISTRY_TOKEN }}
              env:
                REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            - name: Push to Docker Hub
              run: docker push  ${{vars.REGISTRY }}/${{ needs.build.outputs.IMAGE_NAME }}:latest && docker push ${{vars.REGISTRY }}/${{ needs.build.outputs.IMAGE_NAME }}:${{ github.sha }}
